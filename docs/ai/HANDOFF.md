# HANDOFF — Current Repo State

Last updated: 2026-05-19 (Stage 3.5 Atelier Atrium v2 — Silent navigation + contained scenery + integrated metadata)

## Purpose

This file is **current operational state**, not a historical log. It is meant to be loaded into context every session, so it must stay compact. Do not append PR-by-PR history. When something changes, replace or summarize the affected section instead of adding new entries.

## Current product stage

- Roadmap stage: **Stage 3.5 Atelier Atrium v2 — Silent navigation + contained scenery + integrated metadata (2026-05-19).** v2 patch on top of the initial atrium slice (commit `a2b6d7d` on PR #448) after Vercel preview screenshot critique. The initial PR opened the page edge-to-edge but still left three Atelier failures: the SaaS sidebar was visible on Home, destination scenery was leaking as full-page wallpaper, and the active trip dossier exposed orphan status/dates/party metadata as separate UI labels. v2 patch: (A) **Silent navigation** — `AppShell.tsx` is now route-aware on `pathname === "/"`. The standard SaaS `Sidebar` is hidden on Home via `isHomePage ? null : <Sidebar />`; a new `AtelierNavArtifact` floating dock takes its place. `Sidebar` import + `<Sidebar` substring preserved for Phase 8J nav-rescue contract; non-home routes unchanged. (B) **`AtelierNavArtifact.tsx`** (new) — brass-edged paper dock at top-left expanding into a private navigation drawer on click. Full primary + secondary nav, account avatar, `supabase.auth.signOut()`. Keyboard accessible (`aria-expanded`, Escape-to-close, focus rings, scrim button). (C) **Contained scenery** — removed page-wide `<WorldScenery />` mount + import from `DashboardClient.tsx`. Destination scenery is now strictly contained inside the active folio dossier (`.atelier-dossier-scenery` reading `--world-scenery-image` + `--world-scenery` vars) and the room portals (`.world-portal-scenery`). New `.atelier-atrium-neutral` class paints a quiet Alabaster room from `--ds-warm-paper` + `--ds-bone` tokens (no destination wallpaper, no raw hex). `WorldMist` + `WorldAtmosphere` remain as abstract paper-warm ambient layers. (D) **Integrated metadata** — active trip dossier rebuilt as a true full-bleed destination folio. Scenery fills `.atelier-dossier-cover-tall`, title + caption float as on-scrim text with paper drop-shadow inside `.atelier-dossier-cover-content`, and status / dates / travelers / open-folio CTA collapse into a single `.atelier-dossier-scrim` glass-scrim band (backdrop-filter blur 18px, brass hairline ring, paper-glass fill) pinned to the bottom of the cover. No orphan two-column DATES/PARTY grid outside the object. Empty state mirrors the same dossier (Map icon with `bg-ds-accent-subtle` inside the cover + `flex-col sm:flex-row` CTAs inside the scrim). (E) **Quieter typography** — `AtelierGreeting` reduced from a 4.2rem dashboard hero to a tiny italic whisper via `.atelier-greeting-whisper` (display at `clamp(1.05rem, 1.4vw, 1.35rem)`). Required `folio-display` + `folio-issue-eyebrow` + `folio-editorial-sub` + `mapline-rule` primitives preserved at quiet scale. `ConciergeEntry` reduced from giant threshold to small private salon invitation via `.atelier-concierge-artifact` with "The Salon" overline + lantern interior glow `::before`. "01 · Dedicated System" overline kept as `sr-only` for accessibility ledger but no longer visible. (F) **Physical archive** — travel shelf rebuilt as a real curio cabinet with three `.atelier-curio-spine` layers (each with `.atelier-curio-spine-band` brass title band) fanned behind the primary plate, brass shelf rail painted along the bottom edge via `::after`, warm under-shelf shadow via `::before`, ribbed page-edge texture per spine. (G) **Cinematic shelf** — doorway shelf gains brass shelf rail above portals (`.atelier-doorway-shelf-rail` co-classed with `editorial-section-rule` for the 8N-C backwards contract), higher-opacity brass jambs/lintels, warmer interior light pools, crisper scenery filter. (H) **Tests** — `tests/creative-luxury-atrium.test.mjs` extended with section F (13 new tests across 3 sub-suites): silent navigation contracts (sidebar hidden via isHomePage branch, nav artifact mounted, keyboard accessible, routes reachable, CSS shipped with reduced-motion guard), contained-scenery contracts (no `<WorldScenery />` import or mount on Home, neutral Alabaster background reads `--ds-warm-paper` + `--ds-bone` not `var(--world-bg)`, dossier scenery reads `--world-scenery*` vars), integrated-metadata contracts (dossier scrim integrates `TripStatusBadge` + dates + party, curio book spines + brass shelf rail, doorway shelf rail, quiet greeting whisper). `invisible-interface-worlds.test.mjs` E2 + E4 updated to enforce v2 contract: `WorldScenery` import optional, page-wide `<WorldScenery />` forbidden. **2858 total tests, 0 failures.** `npx tsc --noEmit`: clean. `npx next build`: clean (15/15 pages prerendered, Home route ~9 kB). No backend / SQL / provider / env / Supabase / route / data-contract / dependency changes. PR #431 + PR #448 initial contract testids and canonical classes preserved. New testids: `atelier-nav-artifact`, `atelier-nav-dock`, `atelier-nav-drawer`.

- Previous roadmap stage: **Stage 3.5 Atelier Atrium — Edge-to-edge cinematic home (2026-05-19, initial PR #448).** First-principles redesign of the Home page from the World System foundation: the page no longer reads as a box-prison page inside a centered linen canvas. It now bleeds edge-to-edge of the main area, with the destination scenery extending corner to corner, and the page reads as a private boutique atrium. (A) **`AppShell.tsx`** detects the home route via `pathname === "/"` and renders home content WITHOUT the `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` wrapper used by every other route — the home page becomes responsible for its own internal gutters and is free to bleed scenery edge-to-edge of the main area. `mobile-nav-spacer`, `atelier-transition`, and the `mobile-page-content` testid are preserved on both branches (Phase 8J/8M contracts protected). A `home-edge-bleed` class + `data-home-edge-bleed="true"` mark the home wrapper. (B) **New `globals.css` section under INVISIBLE INTERFACE → "ATELIER ATRIUM"** ships 6 reusable atrium primitives, all `--world-*` driven (no hardcoded destination hex): `.home-edge-bleed` (full-width home page wrapper), `.atelier-atrium` + `.atelier-atrium-content` (root + clamp-padded gutters), `.atelier-atrium-hero` (1-col mobile → 2-col desktop hero spread for greeting + dossier), `.atelier-hero-greeting` (modifier for WorldGlassSurface that scales it to magazine-cover proportions), `.atelier-dossier` family (cover/scenery/plate/flag/body/footer + cover-seam at the right edge that glows on hover/focus — the active trip is now a tactile portfolio object, not a card; scenery is clipped inside `.atelier-dossier-cover` with `overflow: hidden` + `isolation: isolate` so it can't leak), `.atelier-concierge-threshold` (a full-width salon doorway with the room interior painted in the right half via `::before` and a brass door-frame `::after`), `.atelier-doorway-shelf` (re-skins `.world-room-switcher` to taller cinematic portals — `min-height: clamp(340px, 36vw, 460px)` on desktop, no more tiny-tile syndrome), `.atelier-curio-shelf` (a physical curio cabinet for the travel archive with three fanned book spines and a brass shelf strip). All motion uses `cubic-bezier(0.16, 1, 0.3, 1)`; all atrium classes ship `@media (prefers-reduced-motion: reduce)` guards. (C) **`DashboardClient.tsx`** rewritten as a true atrium composition: root `<FolioScene>` carries `atelier-atrium` alongside the existing `editorial-scene world-canvas world-scenery-host` classes; `<FolioLivingCanvas>` carries `atelier-atrium-content` so the page owns its own clamp-based gutters; `WorldScenery height="tall"` extends edge-to-edge of the main area (because AppShell no longer wraps the home in `max-w-7xl`); hero spread mounts `AtelierGreeting` (now wrapped in `WorldGlassSurface.atelier-hero-greeting` with editorial display sizing at `2.6–4.2rem`) + the dossier `ContinuePlanningStrip` side-by-side via `.atelier-atrium-hero`; `ConciergeEntry` becomes a full-width `.atelier-concierge-threshold` with a "Step into the salon" CTA; `AtelierPlanningStrip` becomes `.atelier-doorway-shelf` and renders a chapter masthead ("Four doorways. One private house.") above the `WorldRoomSwitcher`; `JourneyShelfTeaser` becomes `.atelier-curio-shelf` with three book-spine pseudo-elements; `EmptyAtelierHome` reads as a blank `.atelier-dossier` rather than a SaaS empty state. The closing `WorldWayfinder` stays a quiet engraved signature under `.atelier-atrium-signature`. (D) **All testid contracts preserved** (`atelier-home`, `atelier-greeting`, `concierge-entry`, `concierge-advisor-desk`, `atelier-continue-planning`, `journey-shelf-teaser`, `atelier-planning-strip`, `atelier-empty-state`, `home-new-trip-action`); all existing canonical class contracts preserved (`folio-paper-card`, `folio-display`, `folio-issue-eyebrow`, `folio-heading`, `folio-card-title`, `folio-caption`, `folio-serial`, `folio-editorial-sub`, `mapline-rule`, `editorial-section-rule`, `editorial-scene`, `btn-marine`, `folio-invitation-panel`, `folio-active-journey-object`, `folio-journey-entry`). New contract tests in `tests/creative-luxury-atrium.test.mjs` cover: AppShell edge-bleed escape hatch (4 tests), DashboardClient atrium composition (11 tests), globals.css atrium primitives (11 tests), scenery containment + portal depth (4 tests), behavior preservation (5 tests). No backend / SQL / provider / env / Supabase / route / data-contract changes. PR #431 protected paths (CityAutocomplete portal, addRoundTripLegToDay, handleAddRoundTripToItinerary, ItineraryItemCard one-way detection) untouched. **2858 total tests, 0 failures.** `npx tsc --noEmit`: clean. `npx next build`: clean (15/15 pages prerendered).

- Previous roadmap stage: **Stage 3.5 Invisible Interface — Home World Immersion correction (2026-05-19).** Visible-adoption correction on top of the World System foundation (PR #446). Previous slice shipped the architecture but the visible Home looked like a decorated linen document — printed "PORTLAND · MISTY FOREST" metadata at the top, abstract gradient blobs but no real scenery, mobile motion fully disabled by `@media (max-width: 600px) { animation: none }`, room portals reading as four labeled tiles, and a hidden `sr-only` legacy artifact stack kept around to preserve stale tests. This correction patch (kept inside the existing World system; no parallel design system created): (A) **`LocationData.visualLayer`** in `frontend/src/lib/worldData.ts` — new `WorldVisualLayer` shape (`imageUrl?`, `imageAlt?`, `imagePosition?`, `imageFilter?`, `sceneryLayers`, `overlay`, `mistTint`) centralizes destination scenery; curated worlds (Portland mist/cedar, Santorini white/caldera, Kyoto shoji/bamboo, Marrakech terra/textile, Lisbon tile/river, Atelier brass) each ship a painted CSS image-like scenic stack plus an optional photographic mood URL replaceable per world. (B) **`applyRoom`** also tints the room portal's `visualLayer` from a new `ROOM_SCENERY` map (atelier=drafting brass, salon=lantern velvet, observatory=open sky, gallery=museum light, scrapbook=warm shelves, residence=textile glow) so each portal feels like a different boutique room. (C) **`worldStyleVars`** publishes the visual layer as `--world-scenery` / `--world-scenery-image` / `--world-scenery-overlay` / `--world-scenery-position` / `--world-scenery-filter` / `--world-mist-tint`. (D) **New components** in `frontend/src/components/ui/World.tsx`: `WorldScenery` (full-bleed environment — painted scenery + optional image + tinted overlay + edge vignette, `tall`/`medium`/`short` heights), `WorldMist` (two drifting tinted veils, mobile-visible), `WorldGlassSurface` (translucent paper/smoke/deep floating panel via `backdrop-filter blur`), `WorldDoorway` (semantic alias for `WorldPortal`). `WorldPortal` upgraded with `.world-portal-scenery`, `.world-portal-doorframe` (lintel + jambs), and `.world-portal-light` (warm interior glow). (E) **`globals.css`** — new `World Scenery / Mist / Glass / Doorway` section under `INVISIBLE INTERFACE`: `.world-scenery` (painted + image + overlay + edge layers; `scale/breathe` and `drift` keyframes; mobile slows but keeps motion; reduced-motion fully halts); `.world-mist` (two veils, screen blend, `world-mist-drift-a/b` keyframes, mobile keeps a slow primary veil); `.world-glass-surface` (paper/smoke/deep tones, blurred backdrop-filter, mobile uses lighter blur); `.world-portal` reskinned as a doorway (dark scenery + warm light pool + brass jambs/lintel + scenery scale on hover); `.world-wayfinder-quiet` modifier for secondary anchor use. (F) **Mobile motion** — previous `@media (max-width: 600px) { .world-atmosphere-blob-a, .world-atmosphere-blob-b { animation: none; } }` removed; mobile now slows blobs (58s/64s) and slows scenery/mist drift but keeps them visible; only `prefers-reduced-motion` fully halts atmosphere. (G) **`DashboardClient.tsx`** — `<FolioScene>` gains `world-scenery-host` class; renders `<WorldScenery height="tall" />` + `<WorldMist />` + `<WorldAtmosphere />` as the first layers, then the in-flow content. `AtelierGreeting` no longer renders the WorldWayfinder as a primary top line — scenery owns orientation; greeting copy is wrapped in `<WorldGlassSurface tone="paper">` so it floats over the scenery. `AtelierPlanningStrip` removed the hidden `sr-only` legacy artifact tiles entirely and adds a quiet "Step through a door" sublabel above the WorldRoomSwitcher. A tiny `world-wayfinder-quiet` editorial signature lives at the bottom as a secondary anchor only. The "PORTLAND · MISTY FOREST…" overline is gone from the visible flow; scenery + atmosphere convey the destination. (H) **Test updates** — `tests/invisible-interface-worlds.test.mjs` extended (visualLayer / WorldVisualLayer / `--world-scenery*` variable contract / WorldScenery+Mist+GlassSurface exports / scenery + mist CSS classes + keyframes + reduced-motion guards / mobile keeps motion / DashboardClient mounts WorldScenery + WorldMist + WorldGlassSurface and no hidden legacy artifact tiles remain). Stale assertions retired: `atelier-home-journey-shelf.test.mjs` now asserts `<WorldRoomSwitcher>` mounted + `ROOM_CATALOGUE` carries `/explore` and `/saved` hrefs (no longer literal in DashboardClient); `visual-world-enforcement-slice4b.test.mjs` test 37 now asserts the AtelierPlanningStrip mounts `WorldRoomSwitcher` instead of counting `folio-paper-card` tiles; `folio-foundation-slice1.test.mjs` test 30 anchored to `.folio-ambient` instead of `lastIndexOf` (so adding mobile-motion world blocks below it can't drift the assertion). All existing testids preserved (`atelier-home`, `atelier-greeting`, `concierge-entry`, `concierge-advisor-desk`, `atelier-continue-planning`, `journey-shelf-teaser`, `atelier-planning-strip`, `atelier-empty-state`, `home-new-trip-action`); all routes preserved; PR #431 protected paths untouched. No backend / SQL / provider / env / Supabase / route / data-contract changes. **2858 total tests, 0 failures.** `npm run type-check`: clean. `npm run build`: clean (15/15 pages prerendered).

- Previous roadmap stage: **Stage 3.5 Home Living Atelier — Editorial Spread (2026-05-19).** Stage 3.5 visual slice on the Home screen only — turned the static linen page into a living editorial spread with desktop asymmetry and tactile mobile rhythm. Twelve new canonical CSS primitives added to `globals.css` under "FOLIO LIVING ATELIER": `.folio-living-canvas` (two slow-drifting warm sunlight blobs, multiply blend, blur 110, 36s/44s drift, prefers-reduced-motion safe), `.folio-luxury-transition` (shared 800ms cubic-bezier(0.16, 1, 0.3, 1) easing token), `.folio-atelier-hero` (editorial hero spread with brass masthead foot rule), `.folio-atelier-invitation` (warm gradient + brass ring + brass corner glow for concierge invitation), `.folio-cta-glide` + `.folio-cta-arrow` (CTA arrow translates 6px on hover/focus with luxury easing), `.folio-active-journey-object` (tactile dossier card with hover lift + box-shadow bloom), `.folio-journey-cover` (painted CSS-only atmospheric cover plate — terracotta wash + sage wash + sand gradient + horizon line, no fake photo), `.folio-journey-stamp` (folio chip stamp inside cover), `.folio-journey-unfurl` (hover/focus-within reveal slot for micro-timeline using only existing trip data), `.folio-artifact-tile` (editorial Explore/Saved tile with tactile lift), `.folio-shelf-spread` (fanned 3-layer paper scrapbook preview with hover fan). Eight new React primitives added to `frontend/src/components/ui/Folio.tsx`: `FolioLivingCanvas`, `FolioAtelierHero`, `FolioAtelierInvitation`, `FolioCtaGlide`, `FolioActiveJourneyObject`, `FolioJourneyCover`, `FolioJourneyUnfurl`, `FolioArtifactTile`, `FolioShelfSpread` — all thin React shells over the canonical CSS classes. `DashboardClient.tsx` rewritten to compose an asymmetric editorial spread on desktop (lg:grid-cols-12 — concierge invitation lg:col-span-7 + active journey lg:col-span-5 with lg:mt-12 vertical offset; lower spread: shelf lg:col-span-7 + discovery artifacts lg:col-span-5 with lg:-mt-4 raise) and a tactile stacked rhythm on mobile (no shrunken dashboard feel). Continue-planning surface gains a painted CSS cover plate, a folio stamp, and a hover/focus-within micro-timeline (dates + travelers only — no fabricated itinerary). Discovery tools become editorial artifact tiles (Explore lifted, Saved offset down on sm+ for uneven editorial rhythm). Shelf preview becomes a fanned paper-edge spread. All luxury motion respects `prefers-reduced-motion`. All existing testids preserved (`atelier-home`, `atelier-greeting`, `concierge-entry`, `concierge-advisor-desk`, `atelier-continue-planning`, `journey-shelf-teaser`, `atelier-planning-strip`, `atelier-empty-state`, `home-new-trip-action`). All existing class contracts preserved (`folio-paper-card`, `folio-paper-panel`, `folio-invitation-panel`, `folio-journey-entry`, `mapline-rule`, `editorial-section-rule`, `folio-serial`, `editorial-scene`, `atelier-transition`, `btn-marine`). **2805 total tests, 0 failures.** `npx tsc --noEmit`: clean. `npx next build`: clean. No backend / SQL / provider / env / Supabase / route / data-contract changes. PR #431 protected paths (CityAutocomplete portal, addRoundTripLegToDay, handleAddRoundTripToItinerary, ItineraryItemCard one-way detection) untouched.

- Previous roadmap stage: **Stage 3.5 Home Mobile Folio Redesign (2026-05-19).** Isolated visual/design slice on Home mobile only — no behavior changes, no other screens touched. Four gaps between current Home and `folio-concept-v1.html` closed: (A) **Editorial serif greeting** — canonical folio-display/folio-issue-eyebrow/folio-editorial-sub primitives applied to AtelierGreeting. (B) **Concierge entry editorial voice** — folio-heading + folio-caption + btn-marine applied; MapPin removed. (C) **Italic editorial captions throughout** — folio-caption on all card subcopy; folio-serial for metadata stamps; folio-card-title on trip names. (D) All orphan folio-home-* classes replaced with canonical Folio primitives. **30 new contract tests in `home-mobile-folio-redesign.test.mjs`. 2729 total tests, 0 failures.** No behavior changes. PR #431 protected paths untouched.

- Previous roadmap stage: **Stage 3.5 Slice 5 — Modal + Cinema Geometry Consistency (2026-05-19).** Post-PR-#441 QA caught two residual coherence gaps the unified architecture didn't yet cover: (1) the Add Note popup inside Trip Itinerary/planning was still rendered with the dark cinema stack (`bg-ds-onyx` panel, `bg-ds-carbon` inputs, `text-ds-text` heading) even though the surrounding canvas is paper-world; (2) cinema-world routes were geometrically inconsistent — Explore wrapped itself in `folio-cinema-lounge` (rounded 1rem frame, brass hairline, atmospheric shadow, mobile edge padding) while `folio-cinema-collection` (Saved) and `folio-cinema-desk` (Concierge) had no rounded corners, no border, and no padding, so the two screens read as sharper all-black panels next to Explore's framed lounge. Fix: (A) **AddNoteModal → paper world** — `TripBuilder.tsx` AddNoteModal panel rewritten: outer panel `bg-ds-paper border border-ds-hairline` with `data-folio-world="paper"`; heading `text-ds-folio-ink`; labels `text-ds-folio-ink-mist`; title + description fields use the canonical `.folio-input` class (warm bone fill, hairline border, dark folio ink, focus-ring marine ink); helper copy uses `text-ds-folio-ink-mist`; Save + Cancel buttons replaced with the canonical `FolioButton` primitive (primary / secondary variants). Bottom-sheet/desktop rounding, all 5 testids (`add-note-modal`, `add-note-modal-panel`, `add-note-title-input`, `add-note-description-input`, `add-note-save-btn`, `add-note-cancel-btn`), title-focus-on-mount, save-disabled-until-title-non-empty, validation, and `handleSaveNote` persistence path all preserved unchanged. (B) **Cinema geometry consistency** — `globals.css`: `.folio-cinema-collection` and `.folio-cinema-desk` extended with the same `border-radius: 1rem`, `border: 1px solid rgba(197, 148, 77, 0.15)`, atmospheric `box-shadow` triple, and (desk) responsive `padding: 1.5rem / 2rem` already used by `.folio-cinema-lounge`. The three cinema-world routes (Explore lounge, Saved collection, Concierge desk) now share one intentional rounded-frame container geometry; Saved's outer wrapper still owns its own mobile edge padding so adding the frame is purely additive. (C) **Guardrails** — `tests/unified-folio-cinema-architecture.test.mjs` extended (section 13): AddNoteModal panel must carry `data-folio-world="paper"`; must not use orphan dark classes (`bg-ds-onyx`, `bg-ds-carbon`, `border-ds-pen-stroke`); must use `text-ds-folio-ink` + `folio-input` + `<FolioButton`; must preserve every testid + focus + disabled-until-title contract. Section 14 asserts `.folio-cinema-lounge` / `.folio-cinema-collection` / `.folio-cinema-desk` all declare the shared rounded-frame radius, brass hairline border, and atmospheric shadow. TripBuilder cream-text ceiling drops from 19 → 8 (only Compare Bar + Toast floating dark overlays remain). Two older guardrail tests updated to assert the new paper-world tokens on TripBuilder (`trip-planning-card-tokens.test.mjs` expects `bg-ds-bone` + `border-ds-hairline` + `bg-ds-linen`; `tripbuilder-command-surface.test.mjs` accepts `bg-ds-bone`/`bg-ds-linen`/`bg-ds-carbon`). **2699 total tests, 0 failures.** `npm run type-check`: clean. `npm run build`: clean. PR #441 unified architecture, PR #431 protected paths (CityAutocomplete portal, addRoundTripLegToDay, handleAddRoundTripToItinerary, ItineraryItemCard one-way detection) untouched. No backend / SQL / provider / env / Supabase / route / dep / note-persistence / itinerary-behavior changes.

- Previous roadmap stage: **Stage 3.5 — Unified Folio/Cinema UI Architecture (2026-05-18).** Architectural consolidation, not another visual slice. Root cause: prior Stage 3.5 slices added CSS vocabulary, but feature files kept hand-rolling local class stacks — paper-world cards using `text-ds-text` (cream) on `bg-ds-bone` (paper), orphan `bg-ds-carbon` pills on paper itinerary chrome, low-contrast `ring-ds-pen-stroke/40` chips. Screenshot-visible failures: invisible "Round-Trip" header in Best Pair flight card, orphan dark "Add to Day 1" pill on itinerary, low-contrast Must-do/Maybe/Skip chips in Trip Ideas. Fix: (A) **Canonical primitive layer** — new `frontend/src/components/ui/Folio.tsx` exports `FolioPage` / `FolioPanel` / `FolioCard` / `FolioSectionHeader` / `FolioInput` / `FolioChip` / `FolioButton` (paper) and `CinemaPage` / `CinemaPanel` / `CinemaCard` / `CinemaChip` (cinema). Each primitive tags its surface with `data-folio-world="paper"|"cinema"`. (B) **Paper-world text-color migration** — `TripBuilder.tsx` (192–2400 candidate-card region), `TripIdeasPanel.tsx`, `ItineraryDayColumn.tsx`, `SearchResultCard.tsx`, `DashboardClient.tsx`, `trips/page.tsx`, `trips/[id]/page.tsx`: every `text-ds-text` → `text-ds-folio-ink`, `text-ds-text-secondary` → `text-ds-folio-ink-soft`, `text-ds-text-tertiary` → `text-ds-folio-ink-mist`, `border-ds-pen-stroke` → `border-ds-hairline`, `bg-ds-carbon` → `bg-ds-linen`, `bg-ds-onyx` (orphan) → `bg-ds-bone`. Floating dark overlays (compare bar, toast, AddNoteModal, trip-detail toast) intentionally preserved as dark. (C) **Orphan dark surfaces fixed** — TripBuilder "Add to Day" pill, "Add Day" button, no-days empty state, SortControl unselected pills, TripIdeasPanel status chips + Show-more buttons, DashboardClient EmptyAtelierHome secondary CTA + loading skeleton. (D) **Architecture guardrails** — new `tests/unified-folio-cinema-architecture.test.mjs` (34 tests) protects: primitives exist + emit data-folio-world; paper-world files have ≤per-file ceiling of cream-text uses (TripBuilder candidate-card region 1–2520 is 0); orphan dark cards forbidden in paper surfaces; cinema-world wrappers in place; PR #431 protected paths (CityAutocomplete portal, addRoundTripLegToDay, handleAddRoundTripToItinerary, ItineraryItemCard round-trip detection) untouched. 6 older test files updated to allow either old or new tokens (architecture-tolerant, not behavior-changing). **2676 total tests, 0 failures.** `npm run type-check`: clean. All PR #431 paths untouched; no backend/SQL/provider/env/Supabase/route/dep changes.

- Previous roadmap stage: **Stage 3.5 Slice 4B — Visual World Enforcement (2026-05-18).** Replaces Slice 4's additive class stacking with single intentional visual compositions on all five cinema-world surfaces. (A) **Paper readability (Scope A)** — `TripBuilder.tsx` Planning cockpit context header: `text-ds-text-tertiary`/`text-ds-text`/`text-ds-accent` → `text-ds-folio-ink-mist`/`text-ds-folio-ink`/`text-ds-marine-ink` (invisible cream tokens on paper background fixed). (B) **Discover visual enforcement (Scope B)** — `ExploreShell.tsx`: wrappers replaced `editorial-scene folio-cinema-shell` → `folio-cinema-lounge`; VerticalCard replaced `folio-cinema-card atelier-surface-depth card-lift boutique-folio` → `folio-cinema-tile`; active section replaced `rounded-xl border border-ds-pen-stroke bg-ds-onyx boutique-instrument` → `folio-cinema-card rounded-xl`. (C) **Saved visual enforcement (Scope C)** — `SavedShell.tsx`: outer shell replaced `saved-clipping-desk folio-cinema-shell` → `folio-cinema-collection`; SavedItemCard replaced `saved-folio-card card-lift` → `folio-collection-card`. (D) **Concierge visual enforcement (Scope D)** — `ConciergePage.tsx`: main wrapper replaced `editorial-scene folio-cinema-shell` → `folio-cinema-desk`; sticky composer replaced `folio-cinema-header boutique-instrument` → `folio-cinema-composer`. (E) **Home cinematic cards (Scope E)** — `DashboardClient.tsx`: ContinuePlanningStrip replaced `Card tone="dark" atelier-surface-depth` → `article.folio-home-cinema-card`; JourneyShelfTeaser + AtelierPlanningStrip links replaced `bg-ds-onyx border-ds-pen-stroke atelier-surface-depth` → `folio-home-cinema-card`; EmptyAtelierHome `text-ds-text`/`text-ds-text-tertiary` → `text-ds-folio-ink`/`text-ds-folio-ink-mist` (paper legibility). 7 new CSS enforcement primitives in `globals.css`: `.folio-cinema-lounge`, `.folio-cinema-tile`, `.folio-cinema-collection`, `.folio-collection-card`, `.folio-cinema-desk`, `.folio-cinema-composer`, `.folio-home-cinema-card`. Unused `Card` import removed from DashboardClient. All PR #431 paths untouched. 50 new Slice 4B contract tests in `visual-world-enforcement-slice4b.test.mjs`. Updated 6 older test files (8nb, 8nc, 8f, 8ne, 8nf, slice4) to reflect superseded assertions. **2612 total tests, 0 failures.**

- Previous roadmap stage: **Stage 3.5 Slice 4 — Legible Paper + Cinematic Concierge World (2026-05-18).** (A) Paper-world legibility: `DashboardClient.tsx`/`trips/page.tsx` cream tokens → folio-ink. (B) Cinema primitives: 6 new CSS classes (`.folio-cinema-shell/card/header/input/result-card`, `.folio-mapline-field`) + 2 tokens applied additively to ConciergePage/ExploreShell/SavedShell. 52 new tests. 2562 total tests, 0 failures.

- Previous roadmap stage: **Stage 3.5 — Paper Planning Objects Slice 3 (2026-05-18).** `ItineraryItemCard`, `TripBuilderForm`, `OptimizeTripModal`, `DayPlanModal` converted to paper world. 46 new tests. 2510 total tests, 0 failures.

- Previous roadmap stage: **Stage 3.5 — Core Paper Planning Surfaces Slice 2 (2026-05-18).** Dashboard/trip cards, itinerary day columns, readiness cockpit, and mobile top chrome converted from dark SaaS to warm Private Travel Atelier paper surfaces. 8 paper primitives added (`folio-paper-card`, `folio-paper-panel`, `folio-paper-section`, `folio-paper-header`, `folio-divider`, `folio-muted-label`, `folio-chip`, `folio-input`). Primary CTAs in paper routes migrated to `btn-marine`/marine-ink. Mobile top bar converted from midnight to bone/hairline (8J test constraint updated). PR #431 logic paths untouched. 92 new Slice 2 contract tests in `core-paper-planning-slice2.test.mjs`.

- Previous roadmap stage: **Stage 3.5 — Folio Foundation Slice 1 (2026-05-18).** Paper-world shell, Fraunces editorial serif, marine ink accent, ambient drift.

- Previous stage: **Stage 3.5 — Post-#430 Portal + Round-Trip Root Cause Fix (2026-05-18).** Two root causes fixed after two failed z-index attempts: (A) `CityAutocomplete` now renders via React DOM `createPortal` into `document.body` with `position:fixed` + `zIndex:9999`, anchored via `getBoundingClientRect`; updates on resize + captured scroll; outside-click checks both `containerRef` and `portalRef`; old `z-[60]+isolate+z-[100]` approach gone. (B) Round-trip leg rendering: `addRoundTripLegToDay` no longer spreads `...d` (was preserving both `outboundLeg`+`returnLeg` on every leg item); `legDetails` built field-by-field with selected leg data + Google Flights URL + provenance only; `ItineraryItemCard` now checks `isExplicitlyOneWay` (`trip_type:"one_way"`, `is_round_trip:false`, `leg_of_round_trip`) before round-trip detection; `handleAddRoundTripToItinerary` uses `extractLegDepartureDate` (12 field paths incl. camelCase/snake/segment) + `resolveItineraryDayByDate`; blind `days[0]`/`days[days.length-1]` fallback removed — missing/mismatched dates show toast and create nothing. No SQL, no env, no provider, no new packages. **2307 total tests, 0 failures.** 43 new contract tests in `fix-failed-posts-portal-roundtrip.test.mjs`; existing test files updated for portal approach and no-fallback behavior.

- Previous stage: **Stage 3.5 — Post-PR #429 Defect Fixes (2026-05-18).** (A) CityAutocomplete `z-[60]+isolate+z-[100]` z-index-only fix (failed on mobile — form ancestor stacking context still covered dropdown). (B) Plan My Day no-cluster path calls canonical Google Places attraction search. 2264 tests, 0 failures. 38 new contract tests in `post-merge-fixes-autocomplete-attractions.test.mjs`.

- Previous stage: **Stage 3.5 — Phase 8N-F (2026-05-18).** Saved Ideas True Visual Correction — `SavedShell.tsx`: `scrapbook-page` fully removed from header zone (was failing cream slab); replaced with `saved-folio-header` (dark integrated, brass-rule only `::before`); `SavedItemCard` article changes from `boutique-folio clipping-card saved-clipping-card` (cream card) to `saved-folio-card` (dark carbon `var(--ds-carbon-mist)` surface + brass top accent `::before`); all `text-ds-text-inverse` (dark ink) on card changed to `text-ds-text` (light on dark); button colors migrated from `bg-ds-bone`/`bg-ds-linen` to `bg-ds-accent-subtle`/`bg-ds-carbon`/`border-ds-pen-stroke` dark-integrated variants; planning bridge buttons now dark-folio pill style; `globals.css`: two new composition classes `.saved-folio-header` (position:relative + brass `::before` rule) and `.saved-folio-card` (`bg: var(--ds-carbon-mist)` + pen-stroke border + box-shadow + brass `::before` top accent strip). Stale cream-class tests removed from 8N-B/8N-C/8N-E test files and replaced with dark-folio assertions. **33 new 8N-F contract tests in `saved-ideas-true-visual-correction-8nf.test.mjs`. 2178 total tests, 0 failures.** No backend/SQL/provider/env/Supabase/API/route/package/behavior changes. All 25+ testids preserved. All handlers preserved (deleteSavedItem, addSavedItemToTrip, listSavedItems, fetchTrips, CreateTripFromSavedModal). Previous Phase 8N-E: outer shell `saved-clipping-desk` preserved; compact horizontal planning bridge preserved; `lg:grid-cols-2` desktop grid preserved; `lg:max-w-4xl` width preserved.

- Previous stage: **Stage 3.5 — Phase 8N-D (2026-05-17).** Old-Format Modal Surface Rescue — Trip Detail modal islands converted from default white SaaS surfaces to boutique atelier dark system. `OptimizeTripModal.tsx`: modal shell changed from `bg-white shadow-2xl` to `advisor-desk-panel` (warm dark gradient + brass accent rule at top + boutique-instrument shadow); header zone uses `concierge-desk-header`; loading spinner changed from `text-sky-400` to `text-ds-accent`; error state uses `text-ds-warning` + `btn-primary`; provider unavailable uses `text-ds-caution` + `btn-ghost`; RANK_BADGE/RANK_BORDER/RANK_BANNER constants migrated from emerald/sky/amber light palette to `bg-ds-carbon border-ds-pen-stroke / border-ds-accent / boutique-instrument / boutique-folio`; scoreColor returns `text-ds-trust / text-ds-caution / text-ds-text-tertiary`; all flight/hotel section labels/dividers/scores use ds tokens; select button uses `btn-primary`; selected state uses `bg-ds-carbon text-ds-trust`; view details uses `btn-ghost`; close button gets `min-h-[44px]` + `aria-label`. `DayPlanModal.tsx`: modal shell changed from `bg-white shadow-2xl` to `advisor-desk-panel`; header zone uses `concierge-desk-header`; attraction cards use `bg-ds-carbon` / `bg-ds-onyx` (added); add buttons changed from `bg-emerald-600` / `bg-rose-600` to `bg-ds-accent text-ds-text-inverse hover:bg-ds-accent-deep`; added state uses `text-ds-trust bg-ds-onyx border-ds-pen-stroke`; dining label uses `text-ds-accent`; UtensilsCrossed uses `text-ds-accent-muted`; star ratings use `fill-ds-caution`; all `border-slate-100`, `text-slate-*`, `text-amber-*`, `text-rose-*` removed; footer border uses `border-ds-pen-stroke`; close button gets `min-h-[44px]` + `aria-label`. New testids: `optimize-trip-modal`, `optimize-loading-state`, `optimize-error-state`, `optimize-results`, `optimize-result-card-{idx}`, `day-plan-modal`, `day-plan-attraction-card`, `day-plan-dining-card`. **60 new 8N-D contract tests in `old-format-modal-surface-rescue-8nd.test.mjs`. 2077 total tests, 0 failures.** No backend/SQL/provider/env/Supabase/API/route/package/behavior changes. All handler functions (`handleSelect`, `run`, `handleAcceptAll`, `handleAdd`, `onAddAttraction`, `onAddRestaurant`, `onClose`, `onPlanSelected`) preserved unchanged.

- Previous stage: **Phase 8N-C SHIPPED (2026-05-17).** Real Boutique Visual Composition Pass — actual editorial composition system added; prior phases were shadow/token-only and insufficient. `globals.css`: 9 new composition primitives: `.editorial-scene` (warm ambient page canvas), `.advisor-desk-panel` + `::before/after` (visible 2-zone desk panel: warm radial glow + brass accent rule at top), `.concierge-desk-header` (darker header zone with warm side-light), `.scrapbook-page` + `::before/after` (warm paper gradient + visible 4px brass binding spine + right page stack), `.folio-cover-tab` (visible 3px brass gradient top accent for folio cards), `.atelier-stamp` (circular postmark mark), `.editorial-section-rule` (warm brass left-weighted divider), `.mapline-rule` (route-line motif), `.clipping-card` (warm white press-clipping card). All classes use CSS tokens/color-mix only (no raw hex). `DashboardClient.tsx`: main wrapper adds `editorial-scene`; ConciergeEntry replaced Card with `advisor-desk-panel boutique-instrument` div — visible 2-zone interior (`concierge-desk-header` zone: darker, `atelier-stamp` mark + overline; body zone: description + CTA); `mapline-rule` after greeting; `editorial-section-rule` before discovery tools. `SavedShell.tsx`: container changed from `bg-ds-linen` to `scrapbook-page` (warm paper gradient + brass binding spine visible); `editorial-section-rule` in header; `SavedItemCard` changed from `Card tone="paper"` to `article.clipping-card` (bright white clipping card on warm paper ground). `trips/page.tsx`: `folio-cover-tab` div at top of JourneyCard (visible brass tab); `advisor-desk-panel concierge-desk-header` on ContinuePlanningHero header; `editorial-scene` on body content. `ConciergePage.tsx`: `editorial-scene` wrapper; `mapline-rule` between header + results; `folio-cover-tab` on ConciergeResultCard (recommendation slip). `TripBuilderForm.tsx`: `editorial-scene` + `advisor-desk-panel` on form element. **50 new 8N-C contract tests in `boutique-visual-composition-8nc.test.mjs`. 1993 total tests, 0 failures.** No backend/SQL/provider/env/Supabase/API/route/package changes. All 8J/8K/8L/8M/8N/8N-B testids preserved.

- Previous stage: **Phase 8N-B + 8N-C SHIPPED (2026-05-17).** 8N-B: Visible Boutique Art Direction Adoption. 8N-C: 9 composition primitives (`advisor-desk-panel`, `scrapbook-page`, etc.) applied to all 7 primary surfaces. 2010 tests, 0 failures.

- Previous stage: **Phase 8N SHIPPED (2026-05-17).** Atmospheric Boutique Art Direction — CSS-only warm atelier atmosphere layer added globally. `globals.css`: 8 new `--ds-atelier-*` custom properties (base, texture-tint, vignette, edge-glow, card-border, surface-highlight, ambient) all in `:root`. New component classes: `.atelier-atmosphere-root` (warm dark base + radial ambient glow), `.atelier-vignette-layer` (fixed CSS vignette, entrance animation), `.atelier-texture-layer` (fixed CSS-only SVG fractalNoise grain via data-URI + `mix-blend-mode: soft-light`), `.atelier-surface-depth` (boutique card edge lighting), `.atelier-transition`, `.shadow-elevation-warm`, `.atelier-accent-line`. All motion guarded by `prefers-reduced-motion`. `AppShell.tsx`: `atelier-atmosphere-root` wrapper + two fixed decorative layers (`atelier-vignette-layer`, `atelier-texture-layer`) with `aria-hidden="true"`; `reduced-motion-safe-atmosphere` testid on `<main>`. 50 new contract tests in `atmospheric-boutique-art-direction-8n.test.mjs`. **1882 total tests, 0 failures.** No backend/SQL/provider/env/Supabase changes. 8J/8K/8L/8M preserved. 8O final cleanup + screenshot certification remains deferred.

- Previous stage: **Phase 8M SHIPPED (2026-05-17).** Whole-site Mobile Surface Pass — Home/Discover/Saved/My Trips/New Trip/Concierge surfaces polished. `TripBuilderForm.tsx`: full ds-token migration (loading state, step icons, info/alert boxes all migrated from legacy sky/slate/emerald/amber/red palette); `data-testid="new-trip-builder-form"` + `new-trip-loading-state` + `new-trip-form-container`; date grid changed from bare `grid-cols-2` to `grid-cols-1 sm:grid-cols-2`; outer containers use `w-full max-w-*`; loading card uses `bg-ds-onyx` surface. `trips/new/page.tsx`: `data-testid="new-trip-page"` wrapper added. `trips/page.tsx`: `data-testid="my-trips-page-header"` + `min-w-0 flex-1` on title div for narrow-screen safety. `ConciergePage.tsx`: sticky search composer changed from `sticky bottom-0 z-10` to `sticky z-10 concierge-sticky-bottom`. `globals.css` (inside `@layer components`): added `.concierge-sticky-bottom` — on mobile clears the fixed bottom nav (`bottom: calc(3.5rem + env(safe-area-inset-bottom, 0px))`); on lg+ restores `bottom: 0`. 54 contract tests in `whole-site-mobile-surface-8m.test.mjs` (wired into `npm test` via `frontend/package.json`). **1832 total tests, 0 failures.** Desktop layout unchanged. No backend/SQL/provider/env/Supabase changes. 8J shell/nav preserved. 8K Trip Detail workspace preserved. 8L itinerary day redesign preserved. 8N atmospheric art direction deferred. 8O final cleanup + screenshot certification deferred.

- Previous stage: **Phase 8L SHIPPED (2026-05-17).** Itinerary Day Mobile Redesign — day chapters + timeline detail + mobile overflow trays. `ItineraryDayColumn.tsx`: added `mobileActionTrayOpen` state; root div gets `data-chapter-id="itinerary-day-mobile-chapter"`; header div gets `data-header-id="itinerary-day-mobile-header"`; Plan My Day + Suggest Timing buttons changed from `flex` to `hidden lg:flex` (hidden on mobile, restored on desktop); added mobile overflow toggle `lg:hidden` button with `data-testid="itinerary-day-mobile-action-tray"` (44px, type=button, aria-label) that opens a `lg:hidden` action tray showing Plan My Day + Suggest Timing; collapsed state div gets `data-testid="itinerary-day-mobile-summary"`; expanded state div gets `data-testid="itinerary-day-mobile-expanded"`; SortableContext child div gets `data-testid="itinerary-day-mobile-timeline"` + mobile-only vertical rail (`lg:hidden absolute w-px bg-ds-pen-stroke/30`) for editorial day-chapter timeline grammar. `ItineraryItemCard.tsx`: added `mobileOverflowOpen` state + `MoreHorizontal` import; content div gets `data-testid="itinerary-item-mobile-timeline-card"`; entry header row gets `data-testid="itinerary-item-mobile-primary-row"`; existing action buttons wrapped in `hidden lg:flex` (hover-reveal preserved on desktop, invisible on mobile); added `lg:hidden` mobile overflow toggle `data-testid="itinerary-item-mobile-overflow-toggle"` (type=button, aria-label, -m-3 p-3 for 44px) opening a `data-testid="itinerary-item-mobile-overflow-actions"` tray with Compare/Timeline/Book/Remove (all min-h-[44px]); "Move to Ideas" moved to a standalone always-visible button (outside both hidden clusters) preserving exact-one-occurrence test contract. 63 contract tests in `itinerary-day-mobile-redesign-8l.test.mjs` (wired into `npm test` via `frontend/package.json`). **1778 total tests, 0 failures.** Desktop layout unchanged. No backend/SQL/provider/env/Supabase/API/route changes. 8M whole-site pass deferred. 8N atmospheric art direction deferred.

- Previous stage: **Phase 8K SHIPPED + patched (2026-05-17).** Trip Detail Mobile Workspace IA Rescue. `trips/[id]/page.tsx`: added `MobileWorkspace` type (`"brief" | "build" | "itinerary" | "ideas"`), `WORKSPACE_TABS` constant, `activeMobileWorkspace` state (default: `"brief"`), mobile-only workspace switcher nav (`lg:hidden`, boutique Pocket Travel Atelier style: overline text, sandstone-gold active indicator bar, 44px touch targets, `type="button"`, ds-token colours); wrapped TripChapterCover + TripReadinessCockpit in `data-testid="trip-mobile-panel-brief"` div (hidden on mobile unless brief, always on desktop); wrapped TripBuilder in a div hidden on mobile when brief is active; passes `mobileWorkspace` prop to TripBuilder. `TripBuilder.tsx`: added `mobileWorkspace?: "build" | "itinerary" | "ideas" | null` prop; left (Build) panel gets `data-testid="trip-mobile-panel-build"` + `hidden lg:flex` when not build workspace; right panel container hidden on mobile when build is active; TripIdeasPanel wrapped in `data-testid="trip-mobile-panel-ideas"` + `hidden lg:block` when not ideas; day columns wrapped in `data-testid="trip-mobile-panel-itinerary"` + `hidden lg:flex` when not itinerary. **Chrome isolation patch:** itinerary header/action row ("Your Itinerary" label, item count, Add-to selector, Add Day button) wrapped in `data-testid="trip-mobile-itinerary-chrome"` + `hidden lg:flex` when `mobileWorkspace === "ideas"`, so the Ideas workspace shows a clean TripIdeasPanel surface on mobile with no itinerary chrome bleeding through; desktop restores via `lg:flex`. Desktop unchanged (lg: breakpoint overrides restore all panels). All existing testids, handlers, DnD, API calls, Google Flights CTA, Compare, Add-to-day, Concierge, edit/delete, and TripIdeasPanel behaviour preserved. 57 contract tests in `trip-detail-mobile-workspace-8k.test.mjs`. **1715 total tests, 0 failures.** No backend/SQL/provider/Tavily/Google Places/Duffel/flights/Supabase/env/API changes. Phase 8J SHIPPED (2026-05-17). Mobile Shell/Nav Rescue — Pocket Travel Atelier design language. Phase 8I also shipped (Mobile-First Premium Pass). Phase 8H (Global Controls) also shipped same day. `globals.css` shared control classes: `.input` migrated to ds-token colors (`var(--ds-pen-stroke)` border, `var(--ds-midnight-ink)` bg, `var(--ds-text-primary)` text, `var(--ds-text-tertiary)` placeholder, `var(--ds-accent-muted)` focus-border); `outline: none` removed; `:focus` → `:focus-visible` with `2px solid var(--ds-accent)` outline; `min-height: 2.75rem` (44px) added. `.select` same pattern. `.label` upgraded to Overline grammar (`var(--ds-type-overline-size/tracking/weight)`, `text-transform: uppercase`, `var(--ds-text-tertiary)`). `.btn-primary` focus-visible raw hex `#e8b854` → `var(--ds-accent)`; `min-height: 2.75rem` added. `.btn-secondary` new class: ds-token ink-on-dark outlined button (`bg-ds-carbon`, `border-ds-pen-stroke`, `text-ds-text-secondary`, `min-height: 2.75rem`, `focus-visible: outline var(--ds-accent)`). `.btn-ghost` raw rgba color migrated to `color-mix(in srgb, var(--ds-pen-stroke) 35/60%, transparent)`; `color: #d8d4ca` → `var(--ds-text-secondary)`; `min-height: 2.75rem`; focus-visible raw hex → `var(--ds-accent)`. Shared UI primitives: `EmptyState.tsx` — `cream-*`/`white/[.]` → `text-ds-text`, `text-ds-text-tertiary`, `bg-ds-carbon`, `border-ds-pen-stroke`. `StatCard.tsx` — `text-cream-*`/`text-emerald-*`/`bg-brand-*` → `text-ds-text`, `text-ds-text-tertiary`, `text-ds-trust`, `text-ds-accent`, `var(--ds-accent-subtle)`. `ResultActionSheet.tsx` — save-action-btn and more-actions-toggle upgraded to `min-h-[44px]` touch targets. `TripIdeasPanel.tsx` — 4 `focus:outline-none focus:ring-1 focus:ring-ds-accent` occurrences replaced with `focus-visible:outline focus-visible:outline-2 focus-visible:outline-ds-accent focus-visible:outline-offset-2`. All behavior, handlers, testids, and page logic preserved. 93 tests in `global-controls-polish-8h.test.mjs` (77 original + 16 patch). **1512 total tests, 0 failures.** No backend/SQL/provider/Tavily/Google Places/Duffel/flights/Supabase/env/API contract changes. Phase 8G correctly described: `SavedShell.tsx` full editorial transformation (saved-scrapbook-header, TYPE_OVERLINES, saved-item-type-overline, saved-item-name, saved-planning-bridge, VerticalGroup → saved-section-${key}) + `CreateTripFromSavedModal.tsx` full ds-token migration. `ExploreShell.tsx` transformed: (1) **Page identity** — Overline "Curated Discovery" + h1 "Discover" editorial framing; (2) **VerticalCard** — legacy sky-500/violet-500/amber-500/emerald-500 palette removed; all four vertical icons unified to sandstone-gold (ds-accent-subtle bg + text-ds-accent); `.card card-lift p-5 group` → `rounded-xl border-ds-pen-stroke bg-ds-onyx shadow-elevation-1 card-lift`; `group-hover:scale-105` forbidden decorative motion removed; Overline category label (VERTICAL_OVERLINES) added above card title; (3) **Active section** — breadcrumb uses ds-text-tertiary/ds-text tokens; `.card p-6` container → `<section> rounded-xl border-ds-pen-stroke bg-ds-onyx shadow-elevation-2`; `explore-instrument-header` div with Overline + heading inside; (4) **data-testid contract** — `explore-lounge-header`, `explore-lounge-breadcrumb`, `explore-instrument-header`. RestaurantExploreFlow/HotelExploreFlow/AttractionExploreFlow: `text-cream-500` → `text-ds-text-tertiary` on form icon colors; result count upgraded from `tracking-wider text-xs` → Overline-correct `tracking-[0.1em] text-[10px] font-semibold`; `data-testid="explore-results-header"` added. All behavior preserved: vertical tabs, searchRestaurants/searchHotelsExplore/searchAttractionsExplore/searchFlightsExplore, ResultActionSheet (save/unsave/expand), Google Maps links, hotel compare CTA, TrustStrip, back navigation, empty/loading/error states, no AI Concierge routing in Hotel/Attraction flows. 99 new tests in `explore-discovery-lounge-8f.test.mjs`. **1284 total tests, 0 failures.** Phase 8E unchanged. `ConciergePage.tsx` and `AIConciergePanel.tsx` transformed: both surfaces now feel like a private travel desk/search instrument, not a chatbot. Key changes: (1) **User turns** — right-aligned chat bubbles replaced with quiet left-border italic query markers (`data-testid="concierge-user-query"` / `concierge-panel-user-query`); (2) **AIConciergePanel header** — generic "AI Concierge" label replaced with editorial Overline "Private Travel Concierge" + destination as named heading, matching standalone page grammar; (3) **Day selector** — label updated from "Target day for Add to Day" → "Add to Day"; Overline tracking corrected `0.08em`→`0.1em`; (4) **Panel loading state** — chatbot bubble ("Researching options…") replaced with instrument-style "Searching · Verifying · Composing"; (5) **Panel input** — `focus:ring-*` replaced with `focus-visible:outline`; `aria-label` + `minHeight: "44px"` added; (6) **Chips** — `rounded-full` chatbot pills → `rounded-lg` instrument chips; (7) **Destination label** — `sr-only` removed; visible Overline "Where" label rendered; (8) **Instruction copy** — "We surface" → "I surface" (first-person concierge voice); (9) **data-testid contract** — 12 testids in ConciergePage, 11 in AIConciergePanel. All behavior preserved: callConciergeSearch, transcript, clear-chat, add-to-day, save, maps, card actions, refinement chips, CONCIERGE_CACHE_VERSION, isRenderableVerifiedPlace. 79 new tests in `concierge-instrument-8e.test.mjs`. **1185 total tests, 0 failures.** Phases 0, 1A–1C, 2A, 2B, 3–7, 8A–8E all shipped.
- Flights v1 — Duffel search-only LIVE: `DUFFEL_FLIGHTS_ENABLED=1`, `DUFFEL_SCHEDULE_TRUST_CERTIFIED=1`, `DUFFEL_DEBUG=false`, `DUFFEL_BOOKING_ENABLED=0`. Each flight card shows "Search on Google Flights" (SEARCH_REDIRECT link-out, not booking). Duffel never creates orders. Ignav DISABLED.
- Active build queue item: **Wife-Wow design system foundation — Phase 8L Itinerary Day Mobile Redesign SHIPPED.** Full Design Bible coverage now spans: shell, nav, explore page, explore cards, saved items, standalone Concierge, trip-context Concierge panel, trip-planning surfaces, itinerary day columns, itinerary item cards, trip detail canvas shell, My Journeys landing dashboard, Trip Readiness Cockpit, home page, **shared control layer**, and **mobile ergonomics** (TripBuilder stacking, HotelExploreFlow/AttractionExploreFlow form rows, TripDetail touch targets + focus-visible, ConciergePage header padding, AIConciergePanel clear-button 44px, ExploreShell responsive padding). See `docs/ai/UI_BASELINE.md` for full phase inventory.
- Current north-star reminder: Discover → Search → Save → Plan → Optimize → Watch. The app must be useful before a trip exists. Wife-wow goal applies. See `docs/product/NORTH_STAR.md`.

## Current architecture / runtime state

- OS v4 is the canonical operating system. No v4.2 or v5 labels.
- **Provider Registry v1** (`backend/app/services/provider_registry.py`) is the single policy source of truth for provider activation, addable-card authority, and disabled/quarantined status. Duffel Flights is now `LINK_OUT` + `production_allowed=True` (Flights v1 active, search-only). Ignav is DISABLED (schedule trust not certified — production smoke test showed externally incorrect schedule times). Skyscanner remains PENDING (access rejected). Future provider additions must register policy + adapter + tests. See `docs/product/DECISION_LOG.md` 2026-05-12.
- Google Places is canonical for addable cards (the only `can_create_addable_cards=True` entry). Yelp / Foursquare / editorial are enrichment / evidence only and cannot mint addable cards. Foursquare is disabled; enrichment is covered by Yelp.
- Brave, Serper, Ignav (flights), Duffel (stays), Amadeus, and Foursquare are disabled/quarantined in the registry. They cannot activate in production even if API keys are present in env. Re-approval in the registry is required to re-enable any of them.
- Duffel Flights is now the active provider; Duffel Stays and Amadeus remain disabled. Ignav may be re-evaluated only after a separate manual schedule-trust certification pass.
- AI Concierge card field contract is the source of truth (`display.displayWhy`, `supportingDetails.whyPick`, top-level `whyPick`).
- **Vertical-search architecture (durable):** Explore and trip creation share canonical Google-Places-backed vertical search services. Explore Hotels → `searchHotelsExplore` → `POST /search/hotels` (`SearchService.search_hotels` → `HotelProvider` Google Places seam). Explore Attractions → `searchAttractionsExplore` → `POST /search/attractions` (`SearchService.search_attraction_results` → `search_attractions` Google Places Text Search). `/trips/create-with-search` seeds hotels/attractions via the same `SearchService` methods/mapping. `/search/restaurants` Google Places path unchanged; flights use the canonical `canonical_flight_search` helper. **The AI Concierge (`/ai/concierge/search`) is NOT the backend for default Explore** — default Explore Hotels/Attractions never call it. Tavily / live research is reserved for the explicit AI Concierge / concierge-note / deep-research path only, further gated by `ALLOW_LIVE_RESEARCH_CALLS`. The PR #368 `allow_live_research` / `allowLiveResearch` flag was a patch around wrong routing and has been fully removed (request model, routes, service, frontend signature).
- **Create Trip from Saved (durable):** create-with-search requires **resolved** origin AND destination airports. A plain saved destination string (e.g. "boise") is never a resolved chip — it stays visible/editable but submit is blocked until the user resolves it via `CityAutocomplete` or a valid IATA code.
- **Round-trip flight add (durable):** adding a canonical round-trip offer now persists **two** standalone one-way flight items — outbound on the outbound departure day, return on the return departure day. Each leg item has `is_round_trip:false` so `ItineraryItemCard` renders them as individual one-way cards. `addRoundTripLegToDay` in `api.ts` handles the split; outbound carries the full price, return carries `round_trip_price_included:true`. Day resolution uses `normalizeIsoDate` on each leg's departure timestamp, falling back to first/last day.
- Latency Budget Pack governs total request-path latency, not just local provider timeouts.
- For long architecture references, read `artifacts/travel_concierge_product_north_star_v3.md`, `artifacts/travel_concierge_v4_travel_os_addendum.md`, `artifacts/ai_concierge_semantic_place_intelligence.md`, and `artifacts/ai_concierge_semantic_place_intelligence_v2_amendment.md` rather than copying them here.
- Runtime workflow guardrails: advisory `.claude/hooks/ai_os_advisory.py` reminds about contract / claim-safety / latency / SQL / env paths. No blocking hooks.

## Recent meaningful PRs

Keep this section small. Only entries that affect future work; replace older lines as they age out.

- 2026-05-18 — **Stage 3.5 Unified Folio/Cinema UI Architecture (PR #441 — design scope: visible adoption).** New `frontend/src/components/ui/Folio.tsx` exports canonical primitives (FolioPage/Panel/Card/SectionHeader/Input/Chip/Button + Cinema variants); each tags surfaces with `data-folio-world`. New `.folio-page` + `.cinema-page` CSS classes back the primitives in `globals.css`. **Real adoption sites:** `DashboardClient` ConciergeEntry → FolioPanel; `TripIdeasPanel` root → FolioPanel, IdeaCard → FolioCard, Show more/less → FolioButton variant=secondary; `ItineraryDayColumn` day-chapter-frame → FolioCard; `TripBuilder` Activities/research panel → FolioPanel; `trips/page.tsx` EmptyDashboard's two action cards → FolioCard; `SearchResultCard` Card primitive switched from tone="dark" to tone="paper" (fixes orphan dark inside paper TripBuilder). Screenshot-visible bugs fixed earlier in the slice (cream-on-paper Round-Trip header, orphan Add-to-Day pill, low-contrast Trip Ideas chips) preserved. New `tests/unified-folio-cinema-architecture.test.mjs` now contains 50 tests covering: primitive exports + data-folio-world markers; per-file cream-text ceilings; TripBuilder candidate-card region zero-leak; no orphan dark cards in paper surfaces; `.folio-page`/`.cinema-page` CSS exist; **feature files actually import + use primitives** (5 files); PR #431 protected paths preserved. 9 older tests relaxed to architecture-tolerant matchers (also accept FolioCard/FolioPanel primitive). **2692 total tests, 0 failures.** Type-check clean. AI PR readiness check passes (`✅ PASS` against `origin/main`). No backend/SQL/provider/env/Supabase/dependency changes. PR #431 logic paths untouched.

- 2026-05-18 — **Stage 3.5 Visual Rescue — Screenshot-Led Composition Fix (PR #440).** CSS value corrections to fix near-zero contrast: `folio-cinema-tile` brighter+::before+overflow:hidden+border 0.26; `folio-cinema-collection` onyx-velvet base; `folio-collection-card` brighter+::before 0.60+border 0.28; `folio-home-cinema-card` ::before+border 0.20; `folio-cinema-desk` warmth 0.13; new `folio-concierge-chip`. DashboardClient 3 overlines → `text-ds-folio-ink-mist`. 30 new tests. 2642 total tests, 0 failures.

- 2026-05-18 — **Stage 3.5 Slice 4B — Visual World Enforcement.** 5 scopes (A–E): replaced additive class stacking with single intentional visual compositions on all cinema-world surfaces. `globals.css`: 7 new enforcement primitives (`.folio-cinema-lounge/tile/collection`, `.folio-collection-card`, `.folio-cinema-desk/composer`, `.folio-home-cinema-card`). `ExploreShell.tsx`, `SavedShell.tsx`, `ConciergePage.tsx`: old stacks replaced. `DashboardClient.tsx`: Card import removed, home cards use `folio-home-cinema-card`, paper text tokens fixed. `TripBuilder.tsx`: Planning header paper tokens fixed. 50 new tests in `visual-world-enforcement-slice4b.test.mjs`; 6 older test files updated. **2612 total tests, 0 failures.** No backend/SQL/provider/env changes.

- 2026-05-18 — **Stage 3.5 Slice 4 — Legible Paper + Cinematic Concierge World.** `DashboardClient.tsx` AtelierGreeting + `trips/page.tsx` EmptyDashboard: cream tokens → folio-ink (paper legibility fix). `globals.css`: 6 new cinema primitives (`.folio-cinema-shell/card/header/input/result-card`; `.folio-mapline-field`) + 2 new `:root` tokens (`--ds-cinema-deep`, `--ds-brass-field-border`). `ConciergePage.tsx`, `ExploreShell.tsx`, `SavedShell.tsx`: cinema classes applied additively alongside existing classes. All PR #431 paths untouched. 52 new tests in `legible-paper-cinematic-slice4.test.mjs`. **2562 total tests, 0 failures.** No backend/SQL/provider/env/behavior/dependency changes.

- 2026-05-18 — **Stage 3.5 Slice 3 — Paper Planning Objects.** `ItineraryItemCard.tsx`, `TripBuilderForm.tsx`, `OptimizeTripModal.tsx`, `DayPlanModal.tsx` converted from dark boutique (`advisor-desk-panel`, `bg-ds-onyx`, `border-ds-pen-stroke`, `text-ds-accent`, `btn-primary`) to paper world (`folio-paper-panel/item/header`, `bg-ds-bone/linen`, `border-ds-hairline`, `text-ds-folio-ink*`, `text-ds-marine-ink`, `btn-marine`, `btn-folio-ghost`). Two new CSS classes (`.folio-paper-item`, `.btn-folio-ghost`) and two shadow tokens (`--ds-paper-elevation-1/2`) added to `globals.css`. Round-trip detection logic untouched. 46 new contract tests in `paper-planning-objects-slice3.test.mjs`. **2510 total tests, 0 failures.** No backend/SQL/provider/env/behavior/dependency changes.

- 2026-05-17 — **Stage 3.5 Phase 8N — Atmospheric Boutique Art Direction.** `globals.css`: 8 `--ds-atelier-*` tokens + `.atelier-atmosphere-root` / `.atelier-vignette-layer` / `.atelier-texture-layer` / `.atelier-surface-depth` / `.atelier-transition` / `.shadow-elevation-warm` / `.atelier-accent-line` CSS-only atmosphere classes. SVG fractalNoise grain via inline data-URI. Radial-gradient vignette entrance animation. All motion guarded by `prefers-reduced-motion`. `AppShell.tsx`: atmosphere-root wrapper + two fixed `aria-hidden` decorative layers + `reduced-motion-safe-atmosphere` testid. 50 new 8N contract tests. **1882 total tests, 0 failures.** No backend/SQL/provider/env/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8M — Whole-site Mobile Surface Pass.** `TripBuilderForm.tsx` full ds-token migration (loading/form/alerts); `new-trip-builder-form` + `new-trip-loading-state` + `new-trip-form-container` testids; `grid-cols-1 sm:grid-cols-2` date row; `trips/new/page.tsx` `new-trip-page` testid; `trips/page.tsx` `my-trips-page-header` testid + `min-w-0 flex-1`; `ConciergePage.tsx` sticky composer fixed via `.concierge-sticky-bottom` CSS class in globals.css (clears mobile bottom nav). 54 new 8M contract tests. **1832 total tests, 0 failures.** No backend/SQL/provider/env/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8L — Itinerary Day Mobile Redesign.** `ItineraryDayColumn.tsx` + `ItineraryItemCard.tsx`: mobile day-chapter UX overhaul. Day columns get `itinerary-day-mobile-chapter/header/summary/expanded/timeline` testids. Secondary day actions (Plan My Day, Suggest Timing) hidden on mobile with `hidden lg:flex`, surfaced through a `itinerary-day-mobile-action-tray` mobile overflow tray. Expanded state gets a subtle vertical rail (`lg:hidden absolute w-px bg-ds-pen-stroke/30`) for editorial timeline grammar. Item cards get `itinerary-item-mobile-timeline-card/primary-row/overflow-toggle/overflow-actions` testids; Compare/Timeline/Book/Remove hidden in `hidden lg:flex` cluster on mobile, accessible through `itinerary-item-mobile-overflow-actions` tray; "Move to Ideas" moved to standalone always-visible location. 63 new 8L contract tests (wired into `npm test`). **1778 total tests, 0 failures.** No backend/SQL/provider/env/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8K — Trip Detail Mobile Workspace IA Rescue (+ chrome isolation patch).** `trips/[id]/page.tsx`: four-tab mobile workspace switcher (`Brief / Build / Itinerary / Ideas`), `activeMobileWorkspace` state, `trip-mobile-workspace` / `trip-mobile-workspace-switcher` / `trip-mobile-tab-*` / `trip-mobile-panel-brief` testids, ds-token boutique switcher (overline text, sandstone-gold indicator bar, 44px touch targets, `type="button"`). `TripBuilder.tsx`: `mobileWorkspace` prop; `trip-mobile-panel-build` / `trip-mobile-panel-itinerary` / `trip-mobile-panel-ideas` testids + `hidden lg:flex`/`hidden lg:block` mobile-only visibility; `trip-mobile-itinerary-chrome` wrapper hides itinerary header/actions on mobile when Ideas is active. Desktop layout unchanged. 57 contract tests. **1715 total tests, 0 failures.** No backend/SQL/provider/env changes. 8L day-card redesign deferred. 8M whole-site pass deferred. 8N atmosphere deferred.

- 2026-05-17 — **Stage 3.5 Phase 8J — Mobile Shell/Nav Rescue (Pocket Travel Atelier).** `MobileNav.tsx`: 5-tab nav replaced with quiet 4-tab boutique nav (Home/Discover/Saved/My Trips); giant center "New Trip" button removed from bottom bar; `tabLinks` reduced to 4 items with static `testid` keys; `drawerLinks` retains all destinations; top bar uses `.mobile-top-bar` (midnight-ink base); bottom nav uses `.mobile-bottom-nav` (midnight + warm brass inset shadow); each tab is `.mobile-tab-item` with CSS-native `env(safe-area-inset-bottom)` safe-area handling; active indicator = `.mobile-tab-active-dot` sandstone bar; `type="button"` on all non-submit buttons. `AppShell.tsx`: `pb-24 lg:pb-8` replaced with `.mobile-nav-spacer` CSS utility; `data-testid="mobile-page-content"` added. `globals.css`: added `.mobile-top-bar`, `.mobile-bottom-nav`, `.mobile-tab-item`, `.mobile-tab-active-dot`, `.mobile-tab-icon/-active`, `.mobile-tab-label/-active`, `.mobile-nav-spacer` (with `@media (min-width: 1024px)` override). `DashboardClient.tsx`: contextual "New" Trip link added to `JourneyShelfTeaser` header (`data-testid="home-new-trip-action"`); empty state Plan a Trip also gets `home-new-trip-action` testid. `trips/page.tsx`: Plan a Trip header link gets `data-testid="trips-new-trip-action"`. `docs/product/MOBILE_DESIGN_LANGUAGE.md` created: defines Pocket Travel Atelier mobile rules (4-tab nav, contextual CTAs, safe-area, touch targets, visual tokens, tray/sheet language for future phases). 95 new contract tests in `mobile-shell-nav-rescue-8j.test.mjs`. **1658 total tests, 0 failures.** No backend/SQL/provider/Tavily/Google Places/Duffel/flights/Supabase/env/API changes. All existing routes, handlers, and testids preserved.

- 2026-05-17 — **Stage 3.5 Phase 8I — Mobile-First Premium Pass.** `TripBuilder.tsx`: main canvas changed from `flex items-start` (broken on mobile — 320px fixed panel left ~54px for itinerary) to `flex flex-col lg:flex-row lg:items-start`; candidate panel `w-80 flex-shrink-0` → `w-full lg:w-80 lg:flex-shrink-0`; compare bar gets `max-w-[calc(100vw-2rem)] sm:bottom-6`. `HotelExploreFlow.tsx`: date/guests form row `grid grid-cols-3` → `grid grid-cols-1 sm:grid-cols-3`. `AttractionExploreFlow.tsx`: search row `flex gap-3` → `flex flex-col gap-3 sm:flex-row`; interest input `w-44 shrink-0` → `sm:w-44 sm:shrink-0`; button `btn-primary shrink-0` → `w-full sm:w-auto sm:shrink-0`. `trips/[id]/page.tsx`: edit-modal close button gets `min-h-[44px] min-w-[44px] flex items-center justify-center`; all 2 legacy `focus:ring-2 focus:ring-ds-accent` instances replaced with `focus-visible:outline` standard; back nav + chapter cover body padding `px-6` → `px-4 sm:px-6`. `ConciergePage.tsx`: header inline `paddingBottom: var(--ds-space-8)` → responsive Tailwind `pb-5 sm:pb-8`. `AIConciergePanel.tsx`: clear button gets `min-h-[44px] flex items-center`. `ExploreShell.tsx`: active section inline `padding: var(--ds-space-6)` → responsive `p-4 sm:p-6`. 51 new contract tests in `mobile-first-premium-8i.test.mjs`. **1563 total tests, 0 failures.** No backend/SQL/provider/Tavily/Google Places/Duffel/flights/Supabase/env/API contract changes.

- 2026-05-17 — **Stage 3.5 Phase 8H + patch (PR #415, merged) — Global Controls, Forms, Action Sheets, and Modal Interaction Polish.** `globals.css`: `.input`/`.select` ds-token migration; `.label` → Overline grammar; `.btn-primary` focus-visible raw hex → `var(--ds-accent)`, `min-height: 2.75rem`; gradient + text color raw hex (`#e8b854`/`#c8a55c`/`#a88438`/`#1a1000`) → `var(--ds-accent)`/`var(--ds-accent-muted)`/`color-mix()`/`var(--ds-text-inverse)`; `.btn-secondary` background fixed from undefined `var(--ds-carbon)` → `var(--ds-carbon-mist)`; `.btn-ghost` raw rgba → `color-mix`. `EmptyState.tsx`/`StatCard.tsx`: cream/brand/emerald → ds-tokens. `ResultActionSheet.tsx`: `save-action-btn` + `more-actions-toggle` → `type="button"` + `min-h-[44px]`; `manage-in-saved-link` → `min-h-[44px]`. `TripIdeasPanel.tsx`: 4× `focus:ring-*` fixed; `type="button"` added to all 9 non-submit buttons (panel toggle, remove, status options, note toggle, assign, filter buttons, reset, show more, show less). 93 tests in `global-controls-polish-8h.test.mjs`. **1512 total tests, 0 failures.** No backend/SQL/provider/Tavily/Supabase/env changes.

- 2026-05-17 — **Stage 3.5 Phase 8G — Saved Ideas Scrapbook + Planning Bridge.** `SavedShell.tsx` full editorial transformation: semantic `<header data-testid="saved-scrapbook-header">` with Overline "Your Travel Scrapbook" + h1 "Saved Ideas" + real collection count; `TYPE_OVERLINES` constant (Restaurant/Attraction/Hotel/Flight); `saved-item-type-overline` Overline per card (tracking-[0.1em], uppercase, text-ds-accent-muted); `saved-item-name` testid on h3; planning bridge section (`saved-planning-bridge`) with "Plan with this" Overline label + border-t hairline + Create Trip + Add to Trip CTA; maps/remove buttons upgraded to `min-w-[44px] min-h-[44px] flex items-center justify-center`; `VerticalGroup` → `saved-section-${key}` / `saved-section-label-${key}` + `border-b border-ds-hairline` editorial section header. `CreateTripFromSavedModal.tsx` full ds-token migration: `bg-dark-100` → `bg-ds-onyx`; `cream-*` → `text-ds-text`/`text-ds-text-tertiary`; `bg-white/[.04]` → `bg-ds-carbon`; `brand-500` → `bg-ds-accent`; `rose-*` → `text-ds-warning`; `amber-300` → `text-ds-caution`; `focus:border-brand-400` → `focus-visible:outline-ds-accent`; close button `min-w-[44px] min-h-[44px]`. 135 new saved-scrapbook-8g contract tests. **1419 total tests, 0 failures.** No backend/SQL/provider/flights/Supabase changes. All existing saved testids preserved.

- 2026-05-17 — **Stage 3.5 Phase 8F — Explore / Discover Curated Search Lounge + patch.** `ExploreShell.tsx` transformed: legacy sky/violet/amber/emerald palette removed; uniform sandstone-gold accent icons; Overline "Curated Discovery" + h1 "Discover" editorial identity; VerticalCard ds-onyx card shell + Overline category label; active section → editorial instrument frame (section + Overline + heading + explore-instrument-header); breadcrumb ds-tokens. HotelExploreFlow/AttractionExploreFlow/RestaurantExploreFlow: cream-500 icon fix; tracking-[0.1em] result count Overline; explore-results-header testid. Patch: hardcoded city examples (Paris/Marais/Barcelona/Tokyo/Shinjuku) removed from placeholders → generic "City or area"/"Destination city"; icon-only Google Maps links → `min-w-[44px] min-h-[44px] flex items-center justify-center` (44px touch target). 114 new tests (99 initial + 15 patch). **1299 total, 0 failures.** No backend/SQL/provider/flights/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8E — Concierge Search Instrument + Results Presentation Overhaul.** `ConciergePage.tsx` + `AIConciergePanel.tsx`: chat bubbles → left-border italic query markers; AIConciergePanel header → editorial overline "Private Travel Concierge" + destination heading; day selector label → "Add to Day"; panel loading → "Searching · Verifying · Composing" instrument style; panel input → focus-visible + aria-label + 44px; chips → rounded-lg; destination label → visible Overline "Where"; copy → first-person. 79 new tests. **1185 total, 0 failures.** No backend/SQL/provider/flights/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8D — Itinerary DayColumn Editorial Chapter Frame.** `ItineraryDayColumn.tsx` transformed: data-testid contract (day-chapter-frame/header/number/title/date/item-count + day-part-section/label + empty-day-chapter); `h3` promoted `text-sm`→`text-base`; day-part sections refactored to `filledSections` with `border-t border-ds-pen-stroke/30` hairline between sections; time hint `italic`; empty day "Begin this chapter" chapter-prompt copy (replaced board-lane "Drag items here… to start building"); Overline tracking `0.08em`→`0.1em` in empty state. All behavior preserved (DnD, SortableContext, groupByDayPart, DAY_PART_META, onPlanDay, handleSuggestTimeline, handleApplyTimeline, onMoveItemToIdeas, ItineraryItemCard rendering). 76 new tests + 5 semantic-button patch tests = 81 total. **1106 total, 0 failures.** Semantic button fix: `day-chapter-header` wrapper div onClick removed; chapter identity converted to `<button type="button">` with aria-label and 44px touch target. No backend/SQL/provider/flights/Supabase changes.

- 2026-05-17 — **Stage 3.5 Phase 8C — Itinerary ItemCard Chapter-Style Editorial Polish.** `ItineraryItemCard.tsx` transformed: root → `<article>`; `TYPE_LABELS` Overline map (Flight/Stay/Activity/Dining/Transit/Note); type icon + Overline label in header row + action cluster; title promoted to `text-[13px]`; type-specific sections gain hairline top separator; padding `p-2`→`p-3`. All behavior preserved (round-trip one-card, Google Flights link-out, DnD, compare, move-to-ideas, timeline, booking). 63 new tests. 1025 total, 0 failures. No backend/SQL/provider/flights/Supabase changes.

- 2026-05-16 — **Stage 3.5 Phase 8B — Trip Detail Travel Chapter + Planning Canvas Overhaul.** `/trips/[id]` transformed: OTA-style destination-color gradients removed; new `TripChapterCover` section (destination h1, trip-title subtitle, italic vibe, dates, 4 semantic action buttons); `TripReadinessCockpit` reworked to advisor briefing (Concierge Notes overline, no KPI score, no dashboard section labels, italic advisor prose). Modal dialogs migrated to ds-tokens. 70 new tests. 962 total, 0 failures. No backend/SQL/provider/flights/Supabase changes.

- 2026-05-16 — **Stage 3.5 Phase 8A — Private Atelier Home + Journey Shelf.** `/` home transformed from KPI dashboard to private travel atelier: editorial time-based greeting + real trip count, Concierge as primary instrument (elevation-2 card), continue-planning strip (real trip data), journey shelf teaser, discovery tools. `/trips` JourneyCard upgraded to travel-volume cover style: destination as large hero text, trip title as semantic link subtitle, editorial footer. Page header gains "Your Travel Shelf" overline. 56 new tests. 892 total, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 7 — Trip Readiness / Review Cockpit.** `TripReadinessCockpit.tsx` created: editorial Trip Briefing section with score indicator, day-coverage pills, 4 category signals (Flights/Stay/Dining/Activities from `itemType`), contextual next-step CTA, Explore/Saved/Concierge quick links. Integrated into `trips/[id]/page.tsx` between PageHeader and TripBuilder. 65 new tests. 836 total, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 6 — Premium My Journeys dashboard.** `trips/page.tsx` rebuilt as journey command center: ContinuePlanningHero, JourneyCard (Card primitive + ds-* tokens + 44px targets), EmptyDashboard editorial hero, PlanningToolsStrip. `DashboardClient.tsx`, `RecentTrips.tsx`, `QuickActions.tsx` fully on ds-* tokens + Card primitive. 41 new tests. 771 tests, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 5 — TripBuilder command surface.** `TripBuilder.tsx`: CandidatePanel → ds-token card + styled count badge. Planning cockpit header (destination + day context). Target day selector in `bg-ds-carbon` container. Add Day button with `min-h-[44px]`. Activities → ds-token. Compare bar → `ds-elevation-4` + Overline "Compare". All `shadow-2xl` → elevation tokens. Overline tracking corrected throughout. 32 new contract tests in `tripbuilder-command-surface.test.mjs`. 724 tests, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 4 — Trip detail planning canvas.** `ItineraryDayColumn.tsx`: full ds-* migration, chapter-style headers, editorial empty-day invitation, 44px touch targets, ink-ladder expanded body. `trips/[id]/page.tsx`: context header + toast + back link migrated. `TripBuilder.tsx`: no-days empty state migrated. 57 new contract tests in `trip-canvas-day-system.test.mjs`. 692 tests, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 3 — Trip planning card system breadth.** Full ds-* token migration: `ItineraryItemCard`, `TripIdeasPanel`, `SearchResultCard`, `TripBuilder` (all 8 card components + shell UI), `FlightExploreFlow`. `SearchResultCard` adopts `Card` primitive with Identity/Meta slots. All top-pick badges → `bg-ds-accent text-ds-text-inverse`. AiScoreBadge → ds-trust-verified/ds-caution. SortControl/FilterPills active → `bg-ds-accent`. Compare bar + toast → `bg-ds-onyx border-ds-pen-stroke`. 24 new tests in `trip-planning-card-tokens.test.mjs`. 614 tests, 0 failures. No backend/SQL/provider changes.

- 2026-05-16 — **Stage 3.5 Phase 2B — Concierge shared presentation and trip-context polish (+ hardening patch).** `frontend/src/lib/concierge/cardHelpers.ts` created: shared `hasClosedSignal` (now scans all 12 fields including `card.raw`), `canShowGoogleVerifiedBadge`, `pickCardMeta` extracted from both `ConciergePage.tsx` and `AIConciergePanel.tsx`. `AIConciergePanel.tsx`: `ConciergeCard` rebuilt with `Card.Identity` / `Card.Trust` / `Card.Why` named slots + `TrustStrip`; all action touch targets raised to 44px; legacy `rgba()`/`rose-*`/`indigo-*`/`slate-*`/`amber-*` replaced with ds tokens (`ds-warning`, `ds-caution`, `color-mix` trust). `ConciergePage.tsx` updated to import from shared module. 27 tests in `concierge-shared-helpers.test.mjs` (21 structural + 6 behavior-level). No backend, SQL, or provider changes. 953 tests, 950 pass (3 pre-existing FlightCard failures).

- 2026-05-16 — **ALLOW_LIVE_RESEARCH_CALLS kill switch enforcement (follow-up to PR #396, Level 2 cost-control).** Production uses `ALLOW_LIVE_RESEARCH_CALLS=false` but `semantic_retrieval_v1` Step 5.56 was not checking it before entering the editorial/Tavily path. Fix: kill switch check added as FIRST statement in Step 5.56's try block; if off, logs `editorial_skipped_reason=allow_live_research_calls_false tavily_attempted=0 serper_attempted=0 brave_attempted=0` and skips all key reads. Google Places fanout (Steps 1–5.5) is unaffected. `Settings.allow_live_research_calls: bool = True` added to pydantic-settings for canonical env→config mapping. 17 new tests in `test_live_research_killswitch.py`. No SQL, no frontend, no new providers.

- 2026-05-16 — **Natural-feature ranking precision gate (PR #396, Level 2 containment, follow-up to #394).** Runtime: "Sunset points with Eiffel tower view" was surfacing "Sunset/Sunside", "Sunset", "Sunset Boulevard" restaurants because name-token match alone gave sf≥0.95. Three-layer fix: (1) **Ranker**: `_subtype_fit()` now filters generic Google types ("point_of_interest", "establishment") before scoring so they can't contribute "point" as a type token; natural-feature name-match suppressor caps name_match=0.15 when entity's specific types are exclusively food/bar/hotel. (2) **Pipeline gate (Step 4.7)**: Hard pre-rank filter in `_run_pipeline()` — for beach/viewpoint/lookout/scenic/waterfall/trail/garden queries, entities with exclusively food/bar/hotel Google types are rejected before ranking; honest empty state (`SOURCE_NONE`) returned when no candidates pass. Gate skips when query has explicit venue head ("beach bars", "sunset cocktail bars") — checked via `literal_ask` token scan. (3) **Editorial gate**: `should_run_editorial()` early-returns False for natural-feature concept labels, stopping Tavily credit spend. Also: `_ATTRACTION_PAT` extended with "beach"/"beaches"/"waterfall"/"garden"/"trail"/"park" so these natural-feature queries route `INTENT_ATTRACTIONS`. 93 new tests in `test_natural_feature_precision_gate.py`; 3568 existing tests pass; 62 pre-existing failures unchanged.

- 2026-05-16 — **Beach/viewpoint place-search stabilization (PR #394, follow-up to #393).** (1) "Points" intent collision fixed: viewpoint/scenic-point compound phrases added to `_ATTRACTION_PAT` so "sunset points", "lookout points", "view points", etc. match before `_REWARDS_PAT`. (2) Query recall fix: `_SYNONYM_EXPANSIONS` extended with beach/viewpoint/sunset/scenic/lookout natural-feature synonyms; `plan_queries` Q2 `pref_primary` comparison moved to concept label (not compound primary) so "best beaches Miami" fans out to 3 queries and compound venue phrases like "sports bars" are not corrupted. (3) 3 pre-existing mock stubs in `TestSetWriterCardCountContract` fixed for `vertical` kwarg drift from PR #393; one assertion updated to match PR #393's "never drop verified cards" behavior. 59 new tests + 121 existing test_semantic_query_frame tests now all pass.

- 2026-05-15 — **AI Concierge vertical-complete place search (PR #393, follow-up to #392).** Broad shared fix across outside-trip and inside-trip Concierge. (1) `_FAST_DYNAMIC_INTENTS` allowlist demoted; `_SEMANTIC_PLACE_SEARCH_BLOCKLIST` (small blocklist) is now the eligibility gate — open-vocabulary categories like beaches, viewpoints, gardens, markets are eligible without per-noun additions. (2) `_detect_semantic_vertical` now routes hotel-noun queries (e.g. "luxury hotels with a view") to hotels bucket even when intent is restaurant-adjacent (INTENT_LUXURY_VALUE). (3) `run_semantic_retrieval_v1` and `_run_pipeline` accept `vertical` param and route cards to correct `LiveResearchResult` bucket. (4) `INTENT_GENERAL` handler consumes `live_result.hotels`; `_semantic_card_first` guard includes hotels. (5) Logging: `destination_source` (explicit_request | trip_context | missing), `place_search_eligible`, `eligibility_reason`, `semantic_card_first_path` added to eligibility and card-first logs. (6) Frontend: lazy `useState(loadPersistedTranscript)` initializer replaces load-on-mount effect, eliminating the re-mount race where `saveTranscript([])` fired before persisted data was set. 102 backend tests; 16 frontend tests. No SQL, no new providers, no design changes.

- 2026-05-15 — **Outside-trip Concierge Stabilization (PR #392, Level 2 backend+frontend).** Added `INTENT_ATTRACTIONS` to `_FAST_DYNAMIC_INTENTS`; user bubbles rendered in ConciergePage.tsx; localStorage transcript persistence with versioned key; clear-chat button. Superseded by PR #393 for vertical-completeness gaps.

- 2026-05-15 — **AI Concierge Notes Runtime Control Plane Fix (Level 2 backend).** Root cause: plain queries like "sports bars" collapsed from 6→1 card because the set-writer skip decision was not propagated to the legacy batched_reason_builder, which ran, produced 1/6 validated notes, and caused `_assemble_card_set` to exclude 5 unvalidated cards. Fix: (1) `NoteDecision` dataclass + `make_note_decision()` in `evidence_cache.py` — single shared gate for ALL optional note/LLM paths. (2) `_assemble_card_reasons` in `semantic_retrieval.py` now takes `note_decision` param; new elif skips legacy batched when `note_decision.should_run_legacy_batched_reasoning=False`. (3) `_assemble_card_set` fixed: never excludes verified Google cards for missing notes — cards with unvalidated notes include the card with hidden note block (`reason_validated=False`). (4) `_effective_note_gen_skipped` expanded to include `note_paths_skipped_no_editorial_evidence`. (5) New ROI telemetry: `legacy_batched_reason_attempted`, `legacy_batched_reason_skipped_reason`, `final_card_count_before_notes`, `final_card_count_after_notes`, `card_count_collapsed_due_to_notes` (structural invariant = False). 31 new tests in `test_concierge_control_plane.py`; 160 existing tests pass. No SQL, no frontend, no provider changes.

- 2026-05-15 — **AI Concierge Durable Evidence/Note Cache v2 (Level 2 backend reliability).** Turns the PR #384 in-memory cache into a two-layer (memory hot + Supabase durable) cache. New `SupabaseEvidenceCache` and `SupabaseNoteCache` in `evidence_cache.py`: atom serialization (EnrichmentAtom ↔ JSONB), 14-day evidence TTL, 30-day note TTL, non-fatal on all Supabase errors. `semantic_retrieval.py` updated: Step 5.56 checks Supabase after memory miss (warms memory on durable hit, skips Tavily); writes to Supabase after Tavily run. Step 5.8 checks Supabase note cache after memory miss; writes to Supabase after LLM approval. New ROI fields: `durable_evidence_cache_hit`, `durable_evidence_cache_write`, `durable_note_cache_hit_count`, `durable_note_cache_write_count`, `durable_cache_error_count`. `app/db/mock.py`: compound key upsert fixed. Migration: `backend/db/migrations/006_concierge_evidence_cache.sql` — manual application required. 48 new tests + 61 PR #384 tests pass. **SQL migration required before production.** Next work: Design Bible visual implementation (Phase 2B or Phase 3) — no more cache work.

- 2026-05-15 — **AI Concierge Evidence/Notes Credit ROI Reliability v1 (Level 2 backend).** New `backend/app/concierge/evidence_cache.py`: in-memory `EvidenceAtomCache` (TTL=3600s), `NoteCache` (TTL=7200s), `CreditROITelemetry` (14 fields). `set_level_writer.py`: `make_cached_note_result()`. `semantic_retrieval.py`: Steps 5.56 + 5.8 cache-aware. No SQL. No new providers. No visible behavior change. 57 tests; 95 existing pass.

- 2026-05-15 — **Stage 3.5 Phase 1C — Saved Ideas Paper/Scrapbook Adoption.** `SavedShell` container → `bg-ds-linen rounded-2xl`. `SavedItemCard` → `Card tone="paper"` (warm paper bg, hairline border, no shadow, luminance hover). `Card.tsx` paper TONE_CLASSES adds `card-paper` modifier. `globals.css` adds `.card-paper` + `.card-paper.card-lift` override (shadow-free, luminance-only lift). All action handlers, payloads, grouping, and behavior unchanged. No provider/API/behavior/SQL changes.

- 2026-05-15 — **Stage 3.5 Phase 1B — Explore Result-Card Adoption.** `RestaurantCard`, `HotelCard`, `AttractionCard` adopt `Card` primitive (dark tone, article element) with Identity/Trust/Meta/Actions slots. `TrustStrip` renders `sourceCount={1}` where Google Place ID confirms provenance (no `verified` — OPERATIONAL status not in payload). Icon accent → sandstone-gold (`ds-accent-subtle` bg + `text-ds-accent`). Text hierarchy → ds-text tokens. Interactive surfaces → `bg-ds-carbon` / hover `bg-ds-pen-stroke`. All action handlers preserved. `ResultActionSheet` button colors migrated to ds-tokens. No provider/API/behavior/SQL changes.

- 2026-05-15 — **Stage 3.5 Phase 1A — Premium App Shell & Navigation Frame.** Shared shell/nav frame upgraded to ds-tokens. Body bg → midnight ink. Sidebar/mobile nav → onyx-velvet surface, pen-stroke borders. Nav items → ds-text-tertiary/accent tokens. Active state → accent-subtle bg + sandstone-gold. Glow blobs removed (forbidden pattern). GSAP shell card entrance removed (420ms exceeded hard limit). Glass removed from shell nav (retained in TripBuilder only, out of scope). `.nav-section-label` Overline CSS class added. `PageHeader.tsx` → ds-text tokens. No provider/API/behavior changes. No SQL. Docs: `UI_BASELINE.md` + `HANDOFF.md` updated.

- 2026-05-14 — **Design Foundation Phase 0.** `globals.css`: `--ds-*` semantic token `:root` block + `@theme` wiring + global reduced-motion rule. `tailwind.config.ts`: minimal Tailwind v4 content config. `Card.tsx`: composable primitive shell (7 slots). `TrustStrip.tsx`: trust signal primitive. No surface adoption. No behavior changes. No SQL.

- 2026-05-14 — **Design Bible Addendum v1.1.** New `docs/product/DESIGN_BIBLE_ADDENDUM_V1_1.md` — concise Stage 3.5 sharpening of Design Bible v1.0 (private atelier principle, Concierge search-bar grammar, trip-as-story model, future experience-lane IA, constraint-first feasibility UX). Docs only — no code, SQL, provider, or env changes.

- 2026-05-14 — **Stage 3 exit / status contract.** Stage 3 declared functionally exited. Canonical provider/search routing locked in `BUILD_QUEUE.md`. Wife-Wow design foundation added as next queue item in `BUILD_QUEUE.md` and `ROADMAP.md` (Stage 3.5). `HANDOFF.md` compacted and updated. No code, SQL, provider, or env changes.

- 2026-05-14 — **PR #370 — Google Hotels compare URL: timezone-safe date formatting + improved date context.** `buildHotelCompareUrl` now formats check-in/check-out dates in local time (not UTC), passes them to the Google Hotels URL. No behavior changes beyond date accuracy.

- 2026-05-14 — **PR #369 — Vertical-search architecture stabilization.** Explore Hotels/Attractions use canonical Google-Places-backed vertical search, not AI Concierge. Removed `allow_live_research` from `ConciergeSearchRequest` and all callers. New `POST /search/attractions` route + `SearchService.search_attraction_results`. `HotelExploreFlow` + `AttractionExploreFlow` rewritten to call `/search/hotels` and `/search/attractions`; no `callConciergeSearch`, no Tavily from default Explore. AI Concierge retains Tavily/live research when explicitly invoked (`ALLOW_LIVE_RESEARCH_CALLS`). No SQL, no env, no new providers.

- 2026-05-14 — **PR #368 — Round-trip add + saved-city resolution stabilization.** (A) "Add Round Trip" now adds one canonical scheduled flight item (`addRoundTripFlightToDay`). `ItineraryItemCard` renders both legs + Google Flights CTA in one card. (B) `CreateTripFromSavedModal` `initFromPrefill` no longer treats plain city text as a resolved chip; submit is blocked until user resolves via `CityAutocomplete`. Note: `allow_live_research` flag from this PR was superseded by PR #369 and fully removed.

- 2026-05-14 — **PR #367 — Hotel compare link-out + Create Trip from Saved autocomplete parity.** `HotelExploreFlow` gains "Compare prices" CTA → deterministic `google.com/travel/hotels` URL (Google Hotels, not Booking.com). `CreateTripFromSavedModal` uses `CityAutocomplete` for origin/destination (matching `TripBuilderForm`). No backend/SQL/env/provider changes.

- 2026-05-14 — **PR #366 — Flight-offer fingerprint dedupe.** Canonical Duffel offers now dedupe by `details.offer_fingerprint` (sha256 over stable fields), not title. 10 distinct round-trip offers → 10 API-visible rows. Backend only. No SQL, no env, no frontend changes.

- Earlier Stage 3 exit blockers (PRs #360–#365): canonical flight card rendering, round-trip dedupe, `is_round_trip` persistence, Saved-item Trip Ideas surfacing, create-with-search canonical flight seeding. All resolved. See `docs/product/DECISION_LOG.md` for durable records.

- Earlier Concierge / provider / save-flow work (Stage 2A, Flights v1, Provider Registry v1): folded into product source-of-truth docs. See `docs/product/DECISION_LOG.md` and `docs/ai/MISS_LEDGER.md`.

## Active invariants / safety packs to remember

Named packs in `docs/ai/SAFETY_PACKS_AND_ARCHETYPES.md` (Travel section) own the rules. The packs themselves are the source of truth — do not paste their contents elsewhere:

- Google Places Addable Authority Pack
- Enrichment Evidence Only Pack
- Semantic Concierge Behavior Pack
- AI Concierge Card Contract Pack
- No Mock/Sample Visible Data Pack
- Latency Budget Pack
- Backend-only Scaffold Pack / No Visible Behavior Change Pack / Test Tier Pack (cross-cutting)

## Type-check / test baseline (confirmed 2026-05-19, Atelier Scene System)

- **`npm run type-check` (frontend): pre-existing errors from missing `node_modules` in remote container — `Cannot find module 'next'` etc. Zero new errors introduced by this PR.** Changed files (`globals.css`, `DashboardClient.tsx`, `Folio.tsx`) have no new TypeScript errors.
- **`npm test` (frontend): 2805 tests, 0 failures.** (includes 66 new home-mobile-atelier-scene contract tests)
- Any future session reporting "pre-existing TypeScript errors" is seeing an uninstalled `node_modules` environment — run `npm install` in `frontend/` before type-check.

## Known risks / unresolved issues

- **Migration 006 must be applied manually** to Supabase before durable evidence/note cache is live in production. Migration: `backend/db/migrations/006_concierge_evidence_cache.sql`. Tables: `concierge_evidence_cache` (14-day TTL) and `concierge_note_cache` (30-day TTL). Until applied, the durable layer gracefully degrades to in-memory only (no error, no behavior change).
- `saved_items` migration 005 must be applied to the Supabase project before Save is live in production. Migration is in `backend/db/migrations/005_saved_items.sql`.
- Hotels Slice 5A is discovery-only (`HotelDiscoveryCard`); do not add rates, prices, availability, or a fake hotel provider. Real hotel offers require a provider-backed Hotel Offer contract (deferred to Stage 2B or later).
- Flights v1 is **live and visible** (2026-05-13): Duffel (`LINK_OUT`, `production_allowed=True`) is trust-certified (`DUFFEL_SCHEDULE_TRUST_CERTIFIED=1`). Trust gate + route/date validation active. Slices default to `max_connections=0` (direct flights). `DUFFEL_DEBUG=false` (cert complete). **Google Flights link-out now supports one-way/round-trip and verified adult passenger count from real tfs fixtures**: `booking_link` is `SEARCH_REDIRECT` (Google Flights tfs= URL) for valid queries; falls back to UNAVAILABLE when URL cannot be built. Encoding verified against 5 real URL samples — field 2=2 for both trip types; round-trip = field 19=1 + two repeated legs; passenger count = repeated field_8=1 per adult. Frontend `FlightCard` shows "Search on Google Flights" — search redirect only, never booking. No OTA/booking URL ever generated. No Duffel orders. Skyscanner PENDING. Ignav DISABLED. Amadeus DISABLED.
- Saved-list foundation (Stage 3 v1) is live; `/saved` page, grouping, remove, and nav links all shipped.
- AI destination intelligence, road trip mode, deal/points intelligence, and Travel Watchtower are deferred to later stages.

## Next recommended step

**Stage 3.5 Slice 5 — Cinema World Surface Refinement.** Remaining work: (A) `ExploreShell` result cards (`RestaurantCard`, `HotelCard`, `AttractionCard`) — still use plain `card/boutique-folio` without `folio-cinema-result-card`; apply cinema skin; (B) `AIConciergePanel` (trip-context) — concierge result cards in trip context should adopt `folio-cinema-result-card` to match standalone ConciergePage; (C) Concierge sticky bottom composer in `AIConciergePanel` — apply `folio-cinema-input` to the panel input field; (D) TripBuilder `CollapsiblePanel` — boutique-folio dark panels in the build canvas could use `folio-cinema-card` treatment. **Hard stop rule**: Slice 5 must not touch PR #431 logic paths or break existing test contracts. Slice 4B is complete — all additive class stacking replaced with intentional compositions.

Active env state: `DUFFEL_API_KEY` + `DUFFEL_FLIGHTS_ENABLED=1` + `DUFFEL_SCHEDULE_TRUST_CERTIFIED=1` + `DUFFEL_BOOKING_ENABLED=0`. Key server-side only; never `NEXT_PUBLIC_`. `IGNAV_FLIGHTS_ENABLED=0`.

## Handoff maintenance rule

- This file is current state only. It is not an append-only log.
- Keep under ~250–500 lines. If it grows past that, **compact before adding** — summarize older sections, do not extend them.
- Every meaningful PR may update this file, but by **replacing or summarizing**, never by appending.
- Move durable historical detail to `docs/ai/MISS_LEDGER.md` (workflow/process misses) or `docs/product/DECISION_LOG.md` (product decisions). Do not preserve old noise just because it exists.
- Do not create new archive files for routine PRs. An archive is justified only when current-state value is being replaced and the original detail is still useful elsewhere.
- Run `python scripts/repo_hygiene_audit.py` before opening cleanup-style PRs and after any major phase. The audit is report-only and flags handoff bloat, banned legacy paths, and uncollected/obsolete tests. See `docs/ai/REPO_HYGIENE.md`.
- `CLAUDE.md`, `docs/ai/AI_REPO_OPERATING_SYSTEM.md`, and `docs/ai/PROMPT_LIBRARY.md` enforce this rule.
