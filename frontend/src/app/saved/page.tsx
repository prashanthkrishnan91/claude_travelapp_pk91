import { SavedShell } from "@/components/saved/SavedShell";

export const metadata = {
  title: "Saved Ideas — Travel Concierge",
  description: "Your saved restaurants, attractions, and hotels — inspiration before a trip.",
};

export default function SavedPage() {
  return <SavedShell />;
}
